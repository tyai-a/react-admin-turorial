import { Card, CardContent, CardHeader } from '@material-ui/core';

export const Top = () => (
    <Card>
        <CardHeader title="ダッシュボード" />
        <CardContent>サイドバーから閲覧したいページを選択します。</CardContent>
    </Card>
);